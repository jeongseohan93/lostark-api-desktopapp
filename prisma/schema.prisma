generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL") // .env 파일의 변수를 사용합니다.
}

model Settings {
  key   String @id
  value String
}